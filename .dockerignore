# Everything below is either rebuilt inside the image or must never enter it.
**/node_modules
**/dist
**/coverage
**/.turbo
.git
.github
.vscode
.idea

# Secrets never enter a build context.
.env
.env.*
!.env.example

# Compose files and docs are not build inputs.
docker-compose*.yml
*.log
storybook-static
