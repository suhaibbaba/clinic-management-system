# Screenshots referenced from pull requests

Captured against the real stack — a seeded database, the built bundle, and object storage held at
900ms per file so a browser has to wait the way a phone would.

| File | What it shows |
| --- | --- |
| `logo-uploaded.png` | The sign-in card with the clinic's uploaded logo |
| `logo-absent.png` | The same card, same box, with the generated initial mark |
| `rail-initial-mark.png` | The sidebar for a clinic that has uploaded nothing |
| `imaging-grid-loading.png` | Every X-ray tile reserved and shimmering, before any file arrives |
| `imaging-grid-loaded.png` | The same grid once they have — identical layout |
