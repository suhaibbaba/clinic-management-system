packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts by default. esbuild needs its install
# script to link the platform binary that Vite and drizzle-kit run on.
onlyBuiltDependencies:
  - esbuild
