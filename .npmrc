# Workspace deps are declared with the `workspace:*` protocol and are always
# linked from the workspace, never fetched from a registry.
link-workspace-packages=true
prefer-workspace-packages=true
# Peer ranges across the Nest/React toolchains are noisy; warn instead of failing installs.
strict-peer-dependencies=false
