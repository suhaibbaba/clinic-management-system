// Typography, not copy. A clinic rewording "جارٍ الحفظ" on the translations screen must not have to
// retype the dots to keep them, and an admin who forgets would leave one screen saying "Saving" and
// the next "Saving…".
export const ellipsis = (text: string): string => `${text}…`;
